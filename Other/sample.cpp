#include <bits/stdc++.h>

using namespace std;

int main() {
    // ios_base::sync_with_stdio(false);
    // cin.tie(0);

    int ord;
    while (cin >> ord)
        cout << char(ord + 65);
    return 0;
}
