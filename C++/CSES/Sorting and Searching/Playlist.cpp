#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> &nums) {
    
    return 0;
}

int main() {
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i ++)
        cin >> nums[i];

    return 0;
}