const str = "Input something......";
console.log(str);

const input = document.querySelectorAll("input");
input.forEach(text => {
    console.log(text);
});

let message: (number|string) = "Hello"
let nums = [1, 1, 2, 3, 5]
